# ┌──────────────────────────────────────────┐
# │  Tmux (wallust generated)      │
# │  DO NOT EDIT — edit the template at      │
# │  wallust/templates/tmux.conf             │
# └──────────────────────────────────────────┘

# ── Prefix ───────────────────────────────────
set -g prefix C-Space
set -g prefix2 C-b
bind C-Space send-prefix

# ── Reload ───────────────────────────────────
bind q source-file ~/.config/tmux/tmux.conf \; display "Config reloaded"

# ── Terminal ─────────────────────────────────
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
set -g mouse on
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g allow-passthrough on
set -g history-limit 50000

# ── Indexing ─────────────────────────────────
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
setw -g aggressive-resize on
set -g detach-on-destroy off

# ── Vi Copy Mode ─────────────────────────────
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# ── Pane Splits ──────────────────────────────
bind v split-window -h -c "#{pane_current_path}"
bind h split-window -v -c "#{pane_current_path}"
bind x kill-pane

# ── Pane Navigation (no prefix) ──────────────
bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

# ── Pane Resize (no prefix) ─────────────────
bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Down resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Right resize-pane -R 5

# ── Window Navigation ────────────────────────
bind c new-window -c "#{pane_current_path}"
bind r command-prompt -I "#W" "rename-window -- '%%'"
bind k kill-window

bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

bind -n M-Left select-window -t -1
bind -n M-Right select-window -t +1
bind -n M-S-Left swap-window -t -1 \; select-window -t -1
bind -n M-S-Right swap-window -t +1 \; select-window -t +1

# ── Session Navigation ───────────────────────
bind C new-session -c "#{pane_current_path}"
bind R command-prompt -I "#S" "rename-session -- '%%'"
bind K kill-session
bind -n M-Up switch-client -p
bind -n M-Down switch-client -n

# ── Status Bar ───────────────────────────────
set -g status-position top
set -g status-interval 5
set -g status-left-length 30
set -g status-right-length 60
set -g window-status-separator ""
setw -g automatic-rename on
setw -g automatic-rename-format '#{b:pane_current_path}'

# ── Theme (wallust generated) ────────────────
set -g status-style "bg=default,fg=default"
set -g status-left "#[fg={{background | strip}},bg={{color4 | strip}},bold] #S #[bg=default] "
set -g status-right "#[fg={{color4 | strip}}]#{?pane_in_mode,COPY ,}#{?client_prefix,PREFIX ,}#{?window_zoomed_flag,ZOOM ,}#[fg={{color8 | strip}}]#h "
set -g window-status-format "#[fg={{color8 | strip}}] #I:#W "
set -g window-status-current-format "#[fg={{color4 | strip}},bold] #I:#W "
set -g pane-border-style "fg={{color8 | strip}}"
set -g pane-active-border-style "fg={{color4 | strip}}"
set -g message-style "bg=default,fg={{color4 | strip}}"
set -g message-command-style "bg=default,fg={{color4 | strip}}"
set -g mode-style "bg={{color4 | strip}},fg={{background | strip}}"
setw -g clock-mode-colour "{{color4 | strip}}"
